name: hackernews
description: training project in making simple hackernews app

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.4
  timeago: ^3.1.0
  flutter_riverpod: ^1.0.3
  favicon: ^1.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  lints: ^1.0.1

flutter:
  uses-material-design: true