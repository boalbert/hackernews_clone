name: hackernews
description: simple hackernews reader

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.4
  timeago: ^3.1.0
  flutter_riverpod: ^1.0.3
  favicon: ^1.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  lints: ^2.0.0

flutter:
  uses-material-design: true